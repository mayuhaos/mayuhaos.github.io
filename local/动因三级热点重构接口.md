> 代码仓库：https://cnb.cool/hangjuxiangmuzu/ComplaintAnalysis/backend-complaint-analysis  （master）
> 前端访问地址：http://mxdemo1.qunl.com:8888
> 用户名：18236517725
> 密码：Gxy132119..

# 首页

### 1.一级热点

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215095846147.png)

#### 🔧 接口名称

热点数据 - 基础热点列表（hotspot/basicList） ^e7a552

---

#### 📋 接口详情

| 项目         | 内容                                                           |
|------------|--------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/hotspot/basicList` |
| **请求方式**   | `GET`                                                        |
| **协议**     | HTTP                                                         |
| **功能说明**   | 获取指定日期范围内的热点问题统计数据，包含态势热点最新热点两类列表                            |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                             |
|----------------|--------|----|-----------------------|--------------------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD）            |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总               |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间（格式：YYYY-MM-DD HH:mm:ss） |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间（格式：YYYY-MM-DD HH:mm:ss） |

---

#### 📤 响应结构

```json
{
  "data": {
    "hotspotList": [ /* 态势热点列表 */ ],
    "newHotspotList": [ /* 最新热点列表 */ ]
  },
  "success": true,
  "message": null
}
```

#### #### 成功响应示例

```json
{
  "data": {
    "hotspotList": [
      { "name": "宽带移机费用争议", "count": 390, "id": "14" },
      { "name": "手机停机欠费", "count": 352, "id": "2" }
    ],
    "newHotspotList": [
      { "name": "宽带带宽", "count": 3, "id": "23" },
      { "name": "套餐订购规则", "count": 4, "id": "25" }
    ]
  },
  "success": true,
  "message": null
}
```

---

### 2.一级热点点击查询

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215101240690.png)

#### 🔧 接口名称

热点数据 - 热点标签分布详情（hotspot/tagDistribution）

---

#### 📋 接口详情

| 项目         | 内容                                                                 |
|------------|--------------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/hotspot/tagDistribution` |
| **请求方式**   | `GET`                                                              |
| **协议**     | HTTP                                                               |
| **功能说明**   | 获取指定热点，在给定时间范围内的标签维度分布数据，包含城市粒度统计、环比变化及今日 Top3 子热点                 |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                                |
|----------------|--------|----|-----------------------|-----------------------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD），用于标识数据快照日     |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总                  |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间（格式：YYYY-MM-DD HH:mm:ss）    |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间（格式：YYYY-MM-DD HH:mm:ss）    |
| `hotspotId`    | string | 是  | `14`                  | 热点唯一标识（对应 `/basicList` 中返回的 `id`） |

---

#### 📤 响应结构

```json
{
  "data": {
    "qoq": 17.12,
    "name": "宽带移机费用争议",
    "count": 390,
    "yesterdayCount": 333,
    "id": "14",
    "todayTop3Hotspots": [ /* 今日Top3子热点 */ ],
    "tagDistribution": { /* 标签维度分布（如901/902等） */ }
  },
  "success": true,
  "message": null
}
```

##### 主体字段说明（`data` 根层级）

| 字段                  | 类型      | 说明                       |
|---------------------|---------|--------------------------|
| `id`                | string  | 热点 ID                    |
| `name`              | string  | 热点名称                     |
| `count`             | integer |                          |
| `yesterdayCount`    | integer | 昨日同期数量                   |
| `qoq`               | number  | 环比增长率（%）                 |
| `todayTop3Hotspots` | array   | 该热点下的今日 Top3 子热点列表       |
| `tagDistribution`   | object  | 按业务标签（如“901”=装维类）分组的详细分布 |

---

##### `todayTop3Hotspots` 子项结构

| 字段               | 类型      | 说明      |
|------------------|---------|---------|
| `id`             | string  | 子热点唯一ID |
| `name`           | string  | 子热点名称   |
| `count`          | integer | 当日数量    |
| `yesterdayCount` | integer | 昨日数量    |
| `volumeScore`    | number  | 体量得分    |
| `qoqScore`       | number  | 环比得分    |
| `totalScore`     | number  | 综合热度得分  |

---

##### `tagDistribution` 结构说明

- **Key**：业务标签编码（如`"901"`,`"902"`），代表不同业务分类（如“装维”、“资费”等）
- **Value**：每个标签下的聚合数

##### 标签对象字段：

| 字段               | 类型      | 说明             |
|------------------|---------|----------------|
| `count`          | integer | 该标签下当日总量       |
| `yesterdayCount` | integer | 昨日总量           |
| `qoq`            | number  | 该标签环比增长率（%）    |
| `ratio`          | number  | 占比             |
| `cities`         | array   | 该标签下 TOP3 城市明细 |

##### `cities` 子项字段：

| 字段               | 类型      | 说明            |
|------------------|---------|---------------|
| `name`           | string  | 城市名称          |
| `count`          | integer | 该城市当日数量       |
| `yesterdayCount` | integer | 昨日数量          |
| `qoq`            | number  | 城市环比增长率（%）    |
| `rank`           | integer | 城市排名（1~3）     |
| `ratio`          | number  | 该城市占本标签总量的百分比 |

---

#### ✅ 成功响应示例（节选关键部分）

```json
{
  "data": {
    "id": "14",
    "name": "宽带移机费用争议",
    "count": 390,
    "yesterdayCount": 333,
    "qoq": 17.12,
    "todayTop3Hotspots": [
      {
        "name": "宽带移机资费争议",
        "count": 6,
        "yesterdayCount": 1,
        "volumeScore": 3.5,
        "qoqScore": 150.0,
        "totalScore": 153.5,
        "id": "2983925"
      }
    ],
    "tagDistribution": {
      "901": {
        "count": 209,
        "yesterdayCount": 196,
        "qoq": 6.63,
        "ratio": 53.59,
        "cities": [
          {
            "name": "宁波市",
            "count": 26,
            "yesterdayCount": 0,
            "qoq": 100.0,
            "rank": 1,
            "ratio": 20.97
          }
        ]
      }
    }
  },
  "success": true,
  "message": null
}
```

### 3.场景态势TOP5

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215155211518.png)

#### 🔧 接口名称

场景预警 - 其他类子场景 Top 列表（sceneWarn/topOther）

---

#### 📋 接口详情

| 项目         | 内容                                                                       |
|------------|--------------------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/sceneWarn/topOther`            |
| **请求方式**   | `GET`                                                                    |
| **协议**     | HTTP                                                                     |
| **功能说明**   | 获取指定预警大类（`code` 前缀，如 `90213`）下的子场景明细，包括名称、数量、日环比变化等，常用于“退订类”“销户类”等业务预警看板 |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                  |
|----------------|--------|----|-----------------------|---------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD） |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总    |
| `code`         | string | 是  | `90213`               | 预警大类编码              |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间              |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间              |

---

#### 📤 响应结构

```json
{
  "data": {
    "sceneWarn": [
      {
        "code": "9021304",
        "name": "退订增值业务",
        "num": 316,
        "dayQoq": "-0.46",
        "hotWords": [],
        "categoryCode": null,
        "city": null,
        "hotspotId": null,
        "id": null,
        "parentUrl": null
      }
    ]
  },
  "success": true,
  "message": null
}
```

#### `sceneWarn` 数组项字段说明

| 字段                                   | 类型      | 说明                           |
|--------------------------------------|---------|------------------------------|
| `code`                               | string  | 子场景唯一编码（如 `9021304`）         |
| `name`                               | string  | 子场景中文名称（如“退订增值业务”）           |
| `num`                                | integer | 当日发生次数（注意：响应中为数字，非字符串）       |
| `dayQoq`                             | string  | 日环比变化率（%），负数表示下降；空字符串表示无昨日数据 |
| `hotWords`                           | array   | 当前为空数组，可能为预留字段               |
| 其他字段（`categoryCode`, `city`, `id` 等） | null    | 当前未使用，可忽略                    |

---

#### ✅ 成功响应示例（节选）

```json
{
  "data": {
    "sceneWarn": [
      {
        "code": "9021304",
        "name": "退订增值业务",
        "num": 316,
        "dayQoq": "-0.46",
        "hotWords": []
      },
      {
        "code": "9021303",
        "name": "退订叠加包",
        "num": 276,
        "dayQoq": "-1.05",
        "hotWords": []
      }
    ]
  },
  "success": true,
  "message": null
}
```

### 4.场景态势弹框

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215133438055.png)

#### 🔧 接口名称

场景预警 - 预警详情页热点词分布（sceneWarn/detail/top）

---

#### 📋 接口详情

| 项目         | 内容                                                            |
|------------|---------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/dataV/sceneWarn/detail/top` |
| **请求方式**   | `GET`                                                         |
| **协议**     | HTTP                                                          |
| **功能说明**   | 获取指定预警编码（`code=9030603`）在某日下的三类热点关键词列表                        |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                  |
|----------------|--------|----|-----------------------|---------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD） |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总    |
| `code`         | string | 是  | `9030603`             | 预警子类唯一编码（通常为 7~8 位） |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间              |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间              |

> 💡 编码规则推测：
> - `903`：服务类大类
> - `90306`：可能为“套餐争议”子类
> - `9030603`：具体到“套餐变更相关争议”场景

---

#### 📤 响应结构

```json
{
  "data": {
    "newRootHotWordByDay": [ /* 新增热点top10 */ ],
    "hotWordTopList": [],     // 热词趋势TOP10
    "rootHotWordList": [      /* 热点趋势top10 */
      {
        "code": "套餐费用争议",
        "dayQoq": "-98.09%",
        "id": "2982294",
        "num": 21,
        "proportion": "N/A",
        "province": null,
        "tagName": "套餐费用争议"
      }
    ]
  },
  "success": true,
  "message": null
}
```

---

#### 🔍 核心字段详解

##### 1. **当日新增热点（`newRootHotWordByDay`）**

| 字段        | 类型      | 说明                                         |
|-----------|---------|--------------------------------------------|
| `tagName` | string  | 热点标签名称（如“SIM卡锁定”）                          |
| `num`     | integer | 当日出现次数                                     |
| `dayQoq`  | string  | 日环比变化率（如 `"400.00%"` 表示激增；`"N/A"` 表示无昨日数据） |
| `id`      | string  | 热点唯一 ID                                    |

##### 2. **历史根因热点（`rootHotWordList`）**

| 字段                 | 说明                      |
|--------------------|-------------------------|
| `tagName` / `code` | 热点名称（两者一致）              |
| `num`              | 当前数量（注意：此处数值较低，如 21、14） |
| `dayQoq`           | 多为大幅下降（如 `-98.09%`）     |

##### 3. **`hotWordTopList`**

- 当前恒为空数组`[]`
- 可能为预留字段（如“实时飙升词”），暂未启用

---

#### ✅ 成功响应特征

- 所有`num`字段为**整数类型**（非字符串）
- `dayQoq`统一为带

---

### 5.总体热点态势

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215153013977.png)

#### 🔧 接口名称

热点数据 - 全量标签分布总览（hotspot/allTagDistribution）

---

#### 📋 接口详情

| 项目         | 内容                                                                    |
|------------|-----------------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/hotspot/allTagDistribution` |
| **请求方式**   | `GET`                                                                 |
| **协议**     | HTTP                                                                  |
| **功能说明**   | 获取指定日期下所有热点的综合统计信息                                                    |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                  |
|----------------|--------|----|-----------------------|---------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD） |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总    |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间              |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间              |

---

#### 📤 响应结构

```json
{
  "data": [
    {
      "id": "14",
      "name": "宽带移机费用争议",
      "count": 390,
      "yesterdayCount": 333,
      "qoq": 17.12,
      "todayTop3Hotspots": [ /* 子热点列表 */ ],
      "tagDistribution": { /* 业务大类分布 */ }
    },
    {
      "id": "2",
      "name": "手机停机欠费",
      "count": 352,
      "yesterdayCount": 235,
      "qoq": 49.79,
      "todayTop3Hotspots": [ ... ],
      "tagDistribution": { ... }
    }
    // ... 其他热点
  ],
  "success": true,
  "message": null
}
```

---

##### 🔍 核心字段详解

###### 1. **热点主信息**

| 字段               | 类型      | 说明                             |
|------------------|---------|--------------------------------|
| `id`             | string  | 热点唯一标识（如 `"14"`）               |
| `name`           | string  | 热点中文名称                         |
| `count`          | integer | 当日总量                           |
| `yesterdayCount` | integer | 昨日总量                           |
| `qoq`            | number  | 日环比增长率（%），如 `17.12` 表示 +17.12% |

###### 2. **今日 Top 3 子热点（`todayTop3Hotspots`）**

每个子项包含：

| 字段                         | 说明                               |
|----------------------------|----------------------------------|
| `id`                       | 子热点 ID（如 `"2982300"`）            |
| `name`                     | 子热点名称                            |
| `count` / `yesterdayCount` | 量级对比                             |
| `volumeScore`              | 体量得分（基于 count 归一化）               |
| `qoqScore`                 | 环比得分（基于增长幅度）                     |
| `totalScore`               | 综合热度分 = `volumeScore + qoqScore` |

###### 3. **业务大类分布（`tagDistribution`）**

以 `{"901": {...}, "902": {...}}` 形式组织，其中：

| 大类编码  | 含义（推测）         |
|-------|----------------|
| `901` | 装维类（安装、移机、故障等） |
| `902` | 计费类（费用、欠费、套餐等） |
| `903` | 服务类（客服、流程、体验等） |
| `904` | 其他/新兴类         |

每个大类对象包含：

| 字段       | 说明          |
|----------|-------------|
| `count`  | 该热点在此大类下的数量 |
| `ratio`  | 占比          |
| `cities` | 前三城市明细      |

###### `cities` 字段结构：

| 字段      | 说明                           |
|---------|------------------------------|
| `name`  | 城市名（如 `"杭州市"`）               |
| `count` | 该城市数量                        |
| `rank`  | 排名（1,2,3）                    |
| `ratio` | 占该大类的比例（如 `49.19` 表示 49.19%） |
| `qoq`   | 城市维度日环比（%）                   |

> ✅ 示例：  
> “宽带移机费用争议”在 `901`（装维类）中占 **53.59%**，其中 **杭州** 占该类 **49.19%**，但环比 **下降 75.4%**。

---

# 热点聚焦

### 1.下拉数据

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215102115088.png)
与首页[一级热点查询接口](#^e7a552)相同

### 2.词云统计

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215102328263.png)

#### 🔧 接口名称

热点数据 - 热点词云列表（hotspot/wordCloud）

---

#### 📋 接口详情

| 项目         | 内容                                                           |
|------------|--------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/hotspot/wordCloud` |
| **请求方式**   | `GET`                                                        |
| **协议**     | HTTP                                                         |
| **功能说明**   | 获取指定业务标签（`tagId`）在某日下的热点关键词及其频次                              |

---

#### 📥 请求参数

| 参数名            | 类型      | 必填 | 示例值                   | 说明                             |
|----------------|---------|----|-----------------------|--------------------------------|
| `yearMonthDay` | string  | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD）            |
| `province`     | string  | 否  | （空）                   | 省份编码或名称，留空表示全国汇总               |
| `tagId`        | string  | 是  | `7`                   | 业务标签 ID（如“欠费类”），需与系统标签体系一致     |
| `startTime`    | string  | 是  | `2025-11-13 00:00:00` | 查询起始时间（格式：YYYY-MM-DD HH:mm:ss） |
| `endTime`      | string  | 是  | `2025-11-13 23:59:59` | 查询结束时间（格式：YYYY-MM-DD HH:mm:ss） |
| `size`         | integer | 否  | `20`                  | 返回词云项数量上限（默认值建议为 20）           |

---

#### 📤 响应结构

```json
{
  "data": [
    {
      "code": "宽带欠费争议",
      "num": "133",
      "id": "2982360"
    }
    // ... 共 size 条
  ],
  "success": true,
  "message": null
}
```

##### `data` 数组项字段说明

| 字段     | 类型     | 说明                      |
|--------|--------|-------------------------|
| `code` | string | 热点关键词文本（用于词云显示）         |
| `num`  | string | 出现频次（注意：当前为字符串类型，实际为整数） |
| `id`   | string | 关键词唯一标识（可用于钻取或关联分析）     |

---

#### ✅ 成功响应示例（节选）

```json
{
  "data": [
    { "code": "宽带欠费争议", "num": "133", "id": "2982360" },
    { "code": "宽带欠费", "num": "71", "id": "2982386" },
    { "code": "宽带欠费停机", "num": "54", "id": "2982345" }
  ],
  "success": true,
  "message": null
}
```

---

### 3.场景态势查询

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215102534430.png)

#### 🔧 接口名称

热点数据 - 关键词标签分布统计（hotspot/distribution）

---

#### 📋 接口详情

| 项目         | 内容                                                              |
|------------|-----------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/hotspot/distribution` |
| **请求方式**   | `GET`                                                           |
| **协议**     | HTTP                                                            |
| **功能说明**   | 获取指定关键词在某日下的业务标签维度分布情况                                          |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                             |
|----------------|--------|----|-----------------------|--------------------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD）            |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总               |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间（格式：YYYY-MM-DD HH:mm:ss） |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间（格式：YYYY-MM-DD HH:mm:ss） |
| `tagId`        | string | 是  | `2982345`             | 关键词唯一标识                        |

---

#### 📤 响应结构

```json
{
  "data": {
    "totalCount": 54,
    "tagDistribution": {
      "901": { /* 标签901的分布数据 */ },
      "902": { /* ... */ }
    }
  },
  "success": true,
  "message": null
}
```

##### `data` 根层级字段说明

| 字段                | 类型      | 说明                      |
|-------------------|---------|-------------------------|
| `totalCount`      | integer | 该关键词在当日的总出现次数           |
| `tagDistribution` | object  | 按业务标签（如 `"901"`）分组的分布明细 |

---

##### `tagDistribution` 结构说明

- **Key**：业务标签编码（字符串，如`"901"`）
- **Value**：该标签下的统计信息

##### 标签对象字段：

| 字段               | 类型      | 说明             |
|------------------|---------|----------------|
| `count`          | integer | 该标签下关键词出现次数    |
| `yesterdayCount` | integer | 昨日同期次数（用于环比计算） |
| `qoq`            | number  | 环比增长率（%）       |
| `ratio`          | number  | 占比             |

---

#### ✅ 成功响应示例

```json
{
  "data": {
    "totalCount": 54,
    "tagDistribution": {
      "901": {
        "qoq": 100.0,
        "count": 41,
        "yesterdayCount": 0,
        "ratio": 75.93
      },
      "902": {
        "qoq": 100.0,
        "count": 5,
        "yesterdayCount": 0,
        "ratio": 9.26
      },
      "903": {
        "qoq": 100.0,
        "count": 1,
        "yesterdayCount": 0,
        "ratio": 1.85
      },
      "904": {
        "qoq": 100.0,
        "count": 7,
        "yesterdayCount": 0,
        "ratio": 12.96
      }
    }
  },
  "success": true,
  "message": null
}
```

---

### 4.热词趋势

- ==没找到接口==
  ![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215103013557.png)

### 5.一级标签查询二级标签

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215110823380.png)

#### 🔧 接口名称

热点数据 - 二级标签明细列表（hotspot/secondLevelTagsDetail）

---

#### 📋 接口详情

| 项目         | 内容                                                                       |
|------------|--------------------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/hotspot/secondLevelTagsDetail` |
| **请求方式**   | `GET`                                                                    |
| **协议**     | HTTP                                                                     |
| **功能说明**   | 获取指定业务大类（`categoryId`）和热点标签（`tagId`）下的二级细分标签（如“费用情况”、“套餐使用情况”等）及其统计分布    |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                             |
|----------------|--------|----|-----------------------|--------------------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD）            |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总               |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间（格式：YYYY-MM-DD HH:mm:ss） |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间（格式：YYYY-MM-DD HH:mm:ss） |
| `tagId`        | string | 是  | `7`                   | 一级热点标签 ID（如“欠费类”）              |
| `categoryId`   | string | 是  | `901`                 | 业务大类 ID（如“装维类”），用于限定二级标签范围     |

---

#### 📤 响应结构

```json
{
  "data": [
    {
      "qoq": 0.0,
      "code": "90101",
      "name": "费用情况",
      "count": 258,
      "yesterdayCount": 0,
      "ratio": 84.31
    }
    // ... 共 N 条二级标签
  ],
  "success": true,
  "message": null
}
```

##### `data` 数组项字段说明

| 字段               | 类型      | 说明                                        |
|------------------|---------|-------------------------------------------|
| `code`           | string  | 二级标签唯一编码（通常为 `categoryId` + 序号，如 `90101`） |
| `name`           | string  | 二级标签中文名称（如“费用情况”）                         |
| `count`          | integer | 当日该二级标签下的事件/工单数量                          |
| `yesterdayCount` | integer | 昨日同期数量                                    |
| `qoq`            | number  | 环比增长率（%），当前示例均为 `0.0`（因昨日为 0）             |
| `ratio`          | number  | 占比                                        |

---

#### ✅ 成功响应示例（节选）

```json
{
  "data": [
    {
      "qoq": 0.0,
      "code": "90101",
      "name": "费用情况",
      "count": 258,
      "yesterdayCount": 0,
      "ratio": 84.31
    },
    {
      "qoq": 0.0,
      "code": "90102",
      "name": "套餐使用情况",
      "count": 18,
      "yesterdayCount": 0,
      "ratio": 5.88
    }
  ],
  "success": true,
  "message": null
}
```

---

### 6.二级标签查询三级标签

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215110852669.png)

#### 🔧 接口名称

热点数据 - 三级标签明细列表（hotspot/thirdLevelTagsDetail）

---

#### 📋 接口详情

| 项目         | 内容                                                                         |
|------------|----------------------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/hotspot/thirdLevelTagsDetail`    |
| **请求方式**   | `GET`                                                                      |
| **协议**     | HTTP                                                                       |
| **功能说明**   | 在指定热点（`tagId`）、业务大类（`categoryId`）和二级标签（`secondLevelId`）条件下，获取最细粒度的三级标签分布数据 |

---

#### 📥 请求参数

| 参数名             | 类型     | 必填 | 示例值                   | 说明                             |
|-----------------|--------|----|-----------------------|--------------------------------|
| `yearMonthDay`  | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD）            |
| `province`      | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总               |
| `startTime`     | string | 是  | `2025-11-13 00:00:00` | 查询起始时间（格式：YYYY-MM-DD HH:mm:ss） |
| `endTime`       | string | 是  | `2025-11-13 23:59:59` | 查询结束时间（格式：YYYY-MM-DD HH:mm:ss） |
| `tagId`         | string | 是  | `14`                  | 一级热点 ID（如“宽带移机费用争议”）           |
| `categoryId`    | string | 是  | `901`                 | 业务大类 ID（如“装维类”）                |
| `secondLevelId` | string | 是  | `90103`               | 二级标签 ID（如“套餐内容”）               |

---

#### 📤 响应结构

```json
{
  "data": [
    {
      "qoq": 0.0,
      "code": "9010304",
      "name": "融合套餐内容",
      "count": 16,
      "yesterdayCount": 0,
      "ratio": 66.67
    },
    {
      "qoq": 0.0,
      "code": "9010302",
      "name": "宽带套餐内容（单产品）",
      "count": 8,
      "yesterdayCount": 0,
      "ratio": 33.33
    }
  ],
  "success": true,
  "message": null
}
```

#### `data` 数组项字段说明

| 字段               | 类型      | 说明                                             |
|------------------|---------|------------------------------------------------|
| `code`           | string  | 三级标签唯一编码（通常为 `secondLevelId` + 序号，如 `9010304`） |
| `name`           | string  | 三级标签中文名称                                       |
| `count`          | integer | 当日该三级标签下的事件/工单数量                               |
| `yesterdayCount` | integer | 昨日同期数量                                         |
| `qoq`            | number  | 环比增长率（%），当前因昨日为 0，显示为 `0.0`                    |
| `ratio`          | number  | 占比                                             |

---

#### ✅ 成功响应示例

```json
{
  "data": [
    {
      "qoq": 0.0,
      "code": "9010304",
      "name": "融合套餐内容",
      "count": 16,
      "yesterdayCount": 0,
      "ratio": 66.67
    },
    {
      "qoq": 0.0,
      "code": "9010302",
      "name": "宽带套餐内容（单产品）",
      "count": 8,
      "yesterdayCount": 0,
      "ratio": 33.33
    }
  ],
  "success": true,
  "message": null
}
```

---

### 7.场景下钻

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215111426413.png)

#### 🔧 接口名称

场景下钻 - 明细记录列表（summary/listByType）

---

#### 📋 接口详情

| 项目         | 内容                                                              |
|------------|-----------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/summary/summary/listByType`   |
| **请求方式**   | `GET`                                                           |
| **协议**     | HTTP                                                            |
| **功能说明**   | 根据指定的热点与多级标签组合（一级热点、业务大类、二级/三级标签等），返回匹配的真实客户会话摘要记录，支持分页查看原始场景数据 |

---

#### 📥 请求参数

| 参数名            | 类型      | 必填 | 示例值                   | 说明                   |
|----------------|---------|----|-----------------------|----------------------|
| `size`         | integer | 否  | `50`                  | 每页返回记录数（默认建议 20~50）  |
| `yearMonthDay` | string  | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD）  |
| `province`     | string  | 否  | （空）                   | 省份编码或名称，留空表示全国       |
| `startTime`    | string  | 是  | `2025-11-13 00:00:00` | 查询起始时间               |
| `endTime`      | string  | 是  | `2025-11-13 23:59:59` | 查询结束时间               |
| `type`         | integer | 是  | `1`                   | 数据类型（1=热点场景，其他值未见使用） |
| `hotOneId`     | string  | 是  | `14`                  | 一级热点 ID（如“宽带移机费用争议”） |
| `hotTwoId`     | string  | 否  | （空）                   | 二级热点 ID（当前未使用，可忽略）   |
| `hotThreeId`   | string  | 否  | （空）                   | 三级热点 ID（当前未使用）       |
| `tag`          | string  | 是  | `901`                 | 业务大类 ID（如“装维类”）      |
| `level2TagId`  | string  | 是  | `90101`               | 二级标签 ID（如“费用情况”）     |
| `level3TagId`  | string  | 是  | `9010109`             | 三级标签 ID（如“调测费争议”）    |

---

#### 📤 响应结构

```json
{
  "data": [
    {
      "connId": "302511132154342109275",
      "general": "客户咨询办理电信宽带并加装电信电视的相关费用及流程...",
      "hotWords": ["宽带加装", "调测费100元", ...],
      "latestTime": "2025-11-13 21:55:17",
      "requirement": "客户询问是否可退机顶盒费用...",
      "solution": "坐席确认安装地址为幸福里3幢1602...",
      "summary": "内容概述：...\r\n客户需求：...\r\n解决方案：..."
    }
  ],
  "success": true,
  "message": null
}
```

##### `data` 数组项字段说明

| 字段                                 | 类型            | 说明                   |
|------------------------------------|---------------|----------------------|
| `connId`                           | string        | 会话唯一标识（可用于追踪原始录音或工单） |
| `latestTime`                       | string        | 会话发生时间（精确到秒）         |
| `general`                          | string        | 会话概要                 |
| `requirement`                      | string        | 客户核心诉求               |
| `solution`                         | string        | 坐席提供的解决方案            |
| `summary`                          | string        | 结构化摘要                |
| `hotWords`                         | array<string> | 提取的热点关键词列表           |
| `code` / `name` / `tagId` / `word` | null          | 当前均为 `null`，可能为预留字段  |

---

#### ✅ 成功响应示例（节选）

```json
{
  "data": [
    {
      "connId": "302511132154342109275",
      "general": "客户咨询办理电信宽带并加装电信电视的相关费用及流程。坐席解释加装需支付100元调测费，年费120元，包含正常电视频道。",
      "hotWords": ["宽带加装", "调测费100元", "电信电视", "幸福里3幢1602", "机顶盒免费租用"],
      "latestTime": "2025-11-13 21:55:17",
      "requirement": "客户询问是否可退机顶盒费用，并确认安装地址及后续流程。",
      "solution": "坐席确认安装地址为幸福里3幢1602，表示将提交申请，工作人员会联系客户二次确认。",
      "summary": "内容概述：...\r\n客户需求：...\r\n解决方案：..."
    }
  ],
  "success": true,
  "message": null
}
```

---

### 8.AI报告生成

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215111617790.png)
调用 场景下钻接口信息，生成报告。

#### 🔧 接口名称

AI报告生成（screenHot/aiReport）

---

#### 📋 接口详情

| 项目         | 内容                                                          |
|------------|-------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/dataV/screenHot/aiReport` |
| **请求方式**   | `POST`                                                      |
| **协议**     | HTTP                                                        |
| **功能说明**   | AI报告生成                                                      |

### 9.聚焦热点

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215111816914.png)

#### 🔧 接口名称

热点数据 - 热点关联词云（hotspot/hotWordsCloud）

---

#### 📋 接口详情

| 项目         | 内容                                                               |
|------------|------------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/hotData/hotspot/hotWordsCloud` |
| **请求方式**   | `GET`                                                            |
| **协议**     | HTTP                                                             |
| **功能说明**   | 获取指定热点关联的高频关键词及其出现频次，用于生成词云或语义分析                                 |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                  |
|----------------|--------|----|-----------------------|---------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD） |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总    |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间              |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间              |
| `hotspotId`    | string | 是  | `2982300`             | 热点唯一标识              |

---

#### 📤 响应结构

```json
{
  "data": {
    "hotWords": [
      { "code": "欠费", "count": 1251 },
      { "code": "营业厅", "count": 927 }
      // ... 共 totalCount 条
    ],
    "totalCount": 50
  },
  "success": true,
  "message": null
}
```

##### 字段说明

| 字段                      | 类型      | 说明                  |
|-------------------------|---------|---------------------|
| `data.hotWords`         | array   | 高频关键词列表             |
| `data.hotWords[].code`  | string  | 关键词文本（如“欠费”、“宽带移机”） |
| `data.hotWords[].count` | integer | 出现频次（数值类型，非字符串）     |
| `data.totalCount`       | integer | 返回关键词总数（当前固定为 50）   |

---

#### ✅ 成功响应示例（节选）

```json
{
  "data": {
    "hotWords": [
      { "code": "欠费", "count": 1251 },
      { "code": "营业厅", "count": 927 },
      { "code": "流量包", "count": 695 },
      { "code": "宽带", "count": 462 }
    ],
    "totalCount": 50
  },
  "success": true,
  "message": null
}
```

---

### 10.热词搜索

#### 🔧 接口名称

热点关键词搜索（dataV/hotPoint/search）

---

#### 📋 接口详情

| 项目         | 内容                                                       |
|------------|----------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/dataV/hotPoint/search` |
| **请求方式**   | `GET`                                                    |
| **协议**     | HTTP                                                     |
| **功能说明**   | 根据输入的关键词（`name`）进行模糊匹配                                   |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                  |
|----------------|--------|----|-----------------------|---------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD） |
| `name`         | string | 是  | `宽带移机费用争议`            | 搜索关键词（中文），支持模糊匹配    |
| `province`     | string | 否  | （空）                   | 省份编码或名称，留空表示全国汇总    |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间              |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间              |

---

#### 📤 响应结构

```json
{
  "data": {
   //目前暂未返回数据
  },
  "success": true,
  "message": null
}
```

# 场景聚焦

### 1.场景详情

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215160350888.png)

#### 🔧 接口名称

场景数据 - 场景详情列表（`sceneInfo/list`）

---

#### 📋 接口详情

| 项目         | 内容                                                      |
|------------|---------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/dataV/sceneInfo/list` |
| **请求方式**   | `GET`                                                   |
| **协议**     | HTTP                                                    |
| **功能说明**   | 获取指定业务场景（通过三级分类编码）在某日的投诉总量、占比、分时段趋势及子类明细数据，用于大屏或分析展示    |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                             |
|----------------|--------|----|-----------------------|--------------------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD）            |
| `province`     | string | 否  | （空）                   | 省份编码，留空表示全国汇总                  |
| `code`         | string | 是  | `9030601`             | 三级分类编码（如“拆机销户”），用于指定具体业务场景     |
| `dayNum`       | int    | 是  | `1`                   | 查询天数（当前仅支持 1）                  |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间（格式：YYYY-MM-DD HH:mm:ss） |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间（格式：YYYY-MM-DD HH:mm:ss） |

---

#### 📤 响应结构

```json
{
  "data": {
    "total": 0,
    "proportion": "0.00%",
    "recentTagNum": [
      {
        "tagName": "2025-11-13 10:00",
        "num": 158
      }
      // ... 每小时一条记录
    ],
    "thirdLevelTag": [
      {
        "code": "90301",
        "name": "网络质量类",
        "num": 0,
        "children": [
          {
            "code": "9030101",
            "name": "信号弱/不稳定",
            "num": 0,
            "parentId": "90301"
          }
          // ... 其他子项
        ]
      }
      // ... 其他一级分类（但仅当前 code 所属路径有数据）
    ]
  },
  "success": true,
  "message": null
}
```

##### 字段说明

| 字段                              | 类型      | 说明                          |
|---------------------------------|---------|-----------------------------|
| `data.total`                    | integer | 当前 `code` 对应场景的总投诉量         |
| `data.proportion`               | string  | 占比（百分比字符串，如 `"0.00%"`）      |
| `data.recentTagNum`             | array   | 按小时粒度的投诉量趋势数据               |
| `data.recentTagNum[].tagName`   | string  | 时间标签（格式：`YYYY-MM-DD HH:00`） |
| `data.recentTagNum[].num`       | integer | 该小时内的投诉数量                   |
| `data.thirdLevelTag`            | array   | 投诉分类树结构（包含从一级到三级的完整嵌套）      |
| `data.thirdLevelTag[].code`     | string  | 分类编码（如 `"90306"` 表示“销户退订类”） |
| `data.thirdLevelTag[].name`     | string  | 分类名称                        |
| `data.thirdLevelTag[].num`      | integer | 该分类下的投诉总数（通常仅叶子节点有意义）       |
| `data.thirdLevelTag[].children` | array   | 子分类列表（递归结构）                 |
| `success`                       | boolean | 请求是否成功                      |
| `message`                       | string  | 错误信息（成功时为 `null`）           |

---

#### ✅ 成功响应示例（节选关键部分）

```json
{
  "data": {
    "total": 0,
    "proportion": "0.00%",
    "recentTagNum": [
      { "tagName": "2025-11-13 10:00", "num": 158 },
      { "tagName": "2025-11-13 11:00", "num": 243 },
      { "tagName": "2025-11-13 16:00", "num": 17 }
    ],
    "thirdLevelTag": [
      {
        "code": "90306",
        "name": "销户退订类",
        "num": 0,
        "children": [
          {
            "code": "9030601",
            "name": "拆机销户",
            "num": 0,
            "parentId": "90306"
          },
          {
            "code": "9030602",
            "name": "业务退订",
            "num": 0,
            "parentId": "90306"
          }
        ]
      }
    ]
  },
  "success": true,
  "message": null
}
```

---

### 2.右侧

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215161337635.png)

#### 🔧 接口名称

场景数据 - 场景根级热点词列表（`sceneRootHotWord/list`）

---

#### 📋 接口详情

| 项目         | 内容                                                             |
|------------|----------------------------------------------------------------|
| **请求 URL** | `http://mxdemo1.qunl.com:8888/api/dataV/sceneRootHotWord/list` |
| **请求方式**   | `GET`                                                          |
| **协议**     | HTTP                                                           |
| **功能说明**   | 获取指定业务场景（通过三级分类编码）下的根级热点关键词及其统计信息，包含整体热点词与新增热点词两部分             |

---

#### 📥 请求参数

| 参数名            | 类型     | 必填 | 示例值                   | 说明                             |
|----------------|--------|----|-----------------------|--------------------------------|
| `yearMonthDay` | string | 是  | `2025-11-13`          | 查询日期（格式：YYYY-MM-DD）            |
| `province`     | string | 否  | （空）                   | 省份编码，留空表示全国汇总                  |
| `code`         | string | 是  | `9030601`             | 三级分类编码（如“拆机销户”），用于指定具体业务场景     |
| `startTime`    | string | 是  | `2025-11-13 00:00:00` | 查询起始时间（格式：YYYY-MM-DD HH:mm:ss） |
| `endTime`      | string | 是  | `2025-11-13 23:59:59` | 查询结束时间（格式：YYYY-MM-DD HH:mm:ss） |

#### 📤 响应结构

目前暂无响应

## 3.折线图

![image.png](https://cdn.jsdelivr.net/gh/mayuhaos/blog-images@main/hj-images/20251215161952216.png)

目前看起来没有接口