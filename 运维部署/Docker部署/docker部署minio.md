```shell
docker commit containerId myminio:latest

docker save -o myminio.tar myminio:latest

```

#### 1. 提交容器为新镜像（如果还没做）

bash

编辑





```
docker commit minio-container myminio:latest
```

------

#### 2. 导出镜像为 tar 包

bash

编辑





```
docker save -o myminio.tar myminio:latest
```

------

#### 3. 上传到服务器

bash

编辑





```
scp myminio.tar user@server-ip:/home/user/
```

------

#### 4. 在服务器上加载镜像

bash

编辑





```
docker load -i myminio.tar
```

------

#### 5. 创建本地数据目录

bash

编辑





```
sudo mkdir -p /data/minio
sudo chown -R 1001:1001 /data/minio
```

------

#### 6. 启动容器并映射路径

bash

编辑





```
docker run -d \
  --name minio \
  -p 9000:9000 \
  -p 9001:9001 \
  -v /data/docker/minio:/data \
  minio/minio \
  server /data \
  --console-address ":9001"
```

> ⚠️ 注意：你必须确保原容器启动命令是 `server /data`，如果不是，要调整





既然容器已经启动且日志没有报错，但 **服务器外部访问不了 MinIO**，这是非常常见的问题，通常不是 MinIO 本身的问题，而是 **网络、防火墙或端口映射配置** 导致的。

------

我们一步步排查，**100% 能解决**。

------

## 🔍 排查步骤清单（按顺序执行）

### ✅ 1. 确认 MinIO 容器是否真的在监听 9000/9001 端口

运行：

bash

编辑





```
docker-compose ps
```

输出应类似：

text

编辑





```
      Name                    Command                  State                    Ports
---------------------------------------------------------------------------------------------------
minio-server     server /data --console-address   Up           0.0.0.0:9000->9000/tcp, 0.0.0.0:9001->9001/tcp
```

✅ 关键点：

- `Ports` 列必须显示 `0.0.0.0:9000->9000/tcp`，表示宿主机的 9000 端口被映射
- 如果是 `127.0.0.1:9000->9000`，则只能本地访问

> ❌ 错误示例：`127.0.0.1:9000->9000` → 只能本机访问，外部无法连接

------

### ✅ 2. 检查服务器是否监听了 9000 和 9001 端口

在服务器上运行：

bash

编辑





```
netstat -tulnp | grep :9000
netstat -tulnp | grep :9001
```

或：

bash

编辑





```
ss -tulnp | grep :9000
```

✅ 正常输出：

text

编辑





```
tcp6 0 0 :::9000 :::* LISTEN 12345/docker-proxy
tcp6 0 0 :::9001 :::* LISTEN 12346/docker-proxy
```

如果 **没有任何输出**，说明端口没映射或没监听。

------

### ✅ 3. 检查服务器防火墙（最常见原因！）

#### 🔹 情况一：使用 `firewalld`（CentOS/RHEL）

bash

编辑





```
# 查看防火墙状态
sudo firewall-cmd --state

# 开放端口
sudo firewall-cmd --permanent --add-port=9000/tcp
sudo firewall-cmd --permanent --add-port=9001/tcp

# 重载防火墙
sudo firewall-cmd --reload

# 查看开放端口
sudo firewall-cmd --list-ports
```